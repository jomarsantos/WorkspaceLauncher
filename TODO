- Overwrite previously created workspaces
- Animation between switching workspaces in edit mode
- Edit previously created workspaces (leverage global vars)
- Enter submit

delete
var array = [2, 5, 9];
var index = array.indexOf(5);
if (index > -1) {
    array.splice(index, 1);
}

rename
a.Prop3 = a.Prop1;
delete a.Prop1;
